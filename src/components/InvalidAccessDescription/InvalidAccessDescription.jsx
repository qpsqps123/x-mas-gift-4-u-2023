import classes from "./InvalidAccessDescription.module.scss";

const InvalidAccessDescription = () => {
  return <p className={classes.invalidAccess}>잘못된 접근입니다!</p>;
};

export default InvalidAccessDescription;
